import AppShell from '@/components/AppShell'
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).single();if(profile?.role!=='admin')redirect('/dashboard');return <AppShell><div className="eyebrow">Acesso reservado</div><h1>Administração</h1><p className="muted">Gerir conteúdos, utilizadores, testes, documentos e atualizações.</p><section className="section grid grid-3"><div className="card"><div className="muted">Utilizadores</div><div className="metric">—</div><p className="muted">Criados através do Supabase Auth.</p></div><div className="card"><div className="muted">Cursos publicados</div><div className="metric">3</div></div><div className="card"><div className="muted">Atualizações pendentes</div><div className="metric">0</div></div></section><section className="section card"><h2>Gestão de conteúdos</h2><table className="table"><thead><tr><th>Área</th><th>Estado</th><th>Ação</th></tr></thead><tbody><tr><td>Nível Iniciante</td><td>Em construção</td><td>Editar</td></tr><tr><td>Terrenos</td><td>Estrutura criada</td><td>Editar</td></tr><tr><td>Radar Legislativo</td><td>Ativo</td><td>Adicionar atualização</td></tr></tbody></table></section></AppShell>}
